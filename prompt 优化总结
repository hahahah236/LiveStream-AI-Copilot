直播复盘工作流：Prompt 调优实录

#### 1. 迭代演进
| 版本 | 发现问题 | 调优对策 | 结果 |
| V1.0 | 违禁词透传 & 策略同质化   | 引入 `is_hot` 逻辑开关；建立违禁词映射表（如：最 $\rightarrow$ 极致性价比）。 | 规避合规风险，实现冷热场话术分层。 |
| V1.1 | 节点逻辑断裂 (Logic Gap) | 建立“线索锚定”协议：强制脚本节点必须读取复盘节点的 `Keyword_Tag`。       | 解决前后矛盾，确保“分析什么，就拍什么”。 |
| V1.2 | 人设漂移 (Degradation)   | 注入“静默指令”：删除所有礼貌用语及反问，设置数据不足时的强停逻辑。  | 彻底去对话化，从“聊天机器人”回归“自动化节点”。 |

#### 2. 核心案例调优

##### A. 解决“逻辑断裂”：确保链路对齐**
* 现象：在全链路测试中，分析节点建议“主打物流”，脚本节点却在“狂轰价格”，导致系统输出前后矛盾。
* 调优：强制复盘节点输出结构化标签（如 `{{Strategy: 物流时效}}`），并规定脚本节点必须以此作为 Hook 的唯一来源。
* 效果：实现了“分析引导生产”的逻辑闭环，产出脚本与复盘结论 100% 匹配。

##### B. 修正“人设漂移”：抑制 AI 的对话冲动**
* 现象：当输入日志条数极少时，AI 会触发“服务模式”，主动询问：“数据较少，请问需要我帮您做什么吗？”
* 调优：注入**“单向执行”**指令。明确规定：若输入数据不足，仅允许输出预设的“未检测到有效日志”并停止，严禁任何形式的交互式反馈。
* 效果：系统在任何环境下均保持专业工具的“冷峻感”，无冗余字符。

##### C. 违禁词的“无感转换”
* 现象：0-Shot 测试发现 AI 会随用户弹幕直接输出“最便宜”、“第一”等违规词。
* 调优：建立“合规映射模块”。要求 AI 在输出前执行一步强制重写，将极限词自动映射为“工厂源头价”、“机制价”等专业表述。
* 效果：输出内容自动合规化，消除了直播间的安全风险。

#### 3. 场景 A/B 回归测试
采用两组截然不同的业务数据进行逻辑验证：
* 数据 A (Hot 场景)**：验证万级在线压力下，AI 是否能保持“极简收割指令”，不产生废话。
* 数据 B (Cold 场景)**：验证低频互动环境下，AI 是否能通过品牌背书逻辑完成转粉引导，且在信息极少时依然守住“自动化节点”人设，不退化为聊天机器人。
